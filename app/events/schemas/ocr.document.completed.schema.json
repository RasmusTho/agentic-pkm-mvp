{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/events/ocr.document.completed.schema.json",
  "title": "ocr.document.completed",
  "type": "object",
  "required": ["event", "trace_id", "uuid", "metrics", "provenance"],
  "properties": {
    "event": { "const": "ocr.document.completed" },
    "trace_id": { "type": "string" },
    "uuid": {
      "type": "string",
      "pattern": "^[0-9a-fA-F-]{36}$"
    },
    "reasons": {
      "type": "array",
      "items": { "type": "string" }
    },
    "metrics": {
      "type": "object",
      "required": ["duration_ms", "pages", "confidence"],
      "properties": {
        "duration_ms": { "type": "number" },
        "pages": { "type": "integer", "minimum": 0 },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
      },
      "additionalProperties": true
    },
    "provenance": {
      "type": "object",
      "required": ["model", "version"],
      "properties": {
        "model": { "type": "string" },
        "version": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
