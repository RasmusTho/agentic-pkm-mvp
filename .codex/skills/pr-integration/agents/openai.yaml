interface:
  display_name: "PR Integration"
  short_description: "Make PR mergeable, CI-green, and review-clean before verification"
  default_prompt: "Use $pr-integration to resolve merge conflicts (verified locally), wait for CI to go green, address review feedback, and produce a handoff-ready PR. Do not merge — verification merges."
