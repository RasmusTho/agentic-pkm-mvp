State: Example / sandbox (not authoritative).
# @Settings — human control panel (example)

These Markdown files are examples. The current runtime does **not** compile them; configuration is via environment variables and code defaults (see `docs/SETTINGS.md`). Keep for future UX ideas and manual notes.
