# panel — Panel artifact-local intent manifestation and confirmation surface.
# Panel is NOT Canvas Core. Panel is NOT Canvas bounded suggestion flow.
# Companion UI does not write vault files directly.
