{
  "title": "memory.fetch.done",
  "type": "object",
  "required": ["event", "agent", "kind", "key", "scope", "hit", "trace_id", "ts"],
  "properties": {
    "event": { "const": "memory.fetch.done" },
    "agent": { "type": "string" },
    "kind": { "type": "string" },
    "key": { "type": "string" },
    "scope": { "type": "string" },
    "hit": { "type": "boolean" },
    "trace_id": { "type": "string" },
    "ts": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": true
}
