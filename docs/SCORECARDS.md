State: Deprecated eval-spec redirect. Scorecard/target guidance now lives in `docs/eval.md`.
# Scorecards (Spec)

This document is retained only as a compatibility stub during the docs cleanup.

Use:
- `docs/eval.md` for eval targets and scorecard-style aspirational metrics
- `docs/TESTING.md` for enforced CI/fitness gates

This file should be removed once inbound links are fully updated.
