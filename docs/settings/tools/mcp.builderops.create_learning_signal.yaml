id: mcp.builderops.create_learning_signal
status: active
protocol: mcp
server: builderops
description: Create a LearningSignal through the controlled BuilderOps boundary.
allowed_args:
  type: object
  additionalProperties: false
  properties:
    summary:
      type: string
    content:
      type: string
    signal_type:
      type: string
    source_refs:
      type: array
    created_by:
      type: object
    idempotency_key:
      type: string
  required: [summary, content, signal_type, source_refs]
mock_result:
  status: ok
  record:
    object_type: LearningSignal
