State: Deprecated operational redirect. Watcher-specific runtime guidance now lives in `docs/OPERATIONS.md`.
# Watcher Operations

This document is retained only as a compatibility stub during the docs cleanup.

Use:
- `docs/OPERATIONS.md` for watcher deployment, config, env vars, and execution-mode guidance
- `docs/OBSERVABILITY.md` for watcher/runtime signal interpretation
- `docs/INFRASTRUCTURE.md` for local Docker/runtime stack details

This file should be removed once inbound links are fully updated.
