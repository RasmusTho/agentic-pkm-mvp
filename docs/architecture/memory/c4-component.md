State: Legacy (archived); memory component view superseded by SoT v4.10.
# C4: Component (memory v4.2)

Historical component diagram referencing a `MemoryStore` with gate policies. Reality-MVP uses `ObjectStore`, `VectorIndex`, and `RelationIndex` per `docs/ARCHITECTURE.md`/`docs/DATA_MODEL.md`. Keep as legacy context only.
