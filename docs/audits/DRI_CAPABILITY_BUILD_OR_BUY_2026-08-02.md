State: Advisory audit snapshot (2026-08-02). Subordinate to `docs/DOCS_INDEX.md` and owner contracts. No executable spec directory; follow-up work is promoted through `docs-to-issue` / `feature-breakdown`, never enacted here.
Doc role: Reference (audit snapshot)
Authority: Evidence-based structural analysis; `file:line` anchors reflect `main` at commit `0b0bb965b` (2026-08-02; verified at `c3c673c51`, and the two intervening commits — #4561 merge-driver, #4564 docs-header repair — touch no audited surface). Where this audit and an owner doc disagree, the owner doc wins and the divergence is raised via issue, not silently resolved. This audit changes no SBS, owner-doc, code, or runtime state.

# DRI (Derived Representation & Indexing) — Capability-Driven Build-or-Buy Audit

Date: 2026-08-02
Governing issue: #3539 (origin: model-inquiry `inq_20260712T175416Z_0232a66f`)
Basis: read-only survey of `app/stores/*`, `app/store/*`, `app/index/*`, `app/indexer/consumer.py`, `app/retrieval/*`, `app/ingest/{api.py,vault_alpha.py,chunk_policy.py}`, `app/services/indexer.py`, `app/workers/outbox_worker.py`, `app/search/service.py`, `app/llm/{embed_queue.py,fallback_orchestrator.py}`, `app/components/embeddings/*`, `app/promotion/gates.py`, `app/fitness/metrics.py`, `mimer_runtime/dri.py`, `schemas/*`, `tests/invariants/*`, `tests/index/*`, against `docs/SYSTEM_BREAKDOWN_STRUCTURE.md`, `docs/boundaries/DRI.md`, `docs/architecture/SBS_{OPERATING_MODEL,CURRENT_TO_TARGET_MAPPING,TRANSITION_DEBT}.md`, `docs/testing/invariant-tests.md`, `docs/DB_SCHEMA.md`, `docs/EMBEDDINGS.md`.

Method: INCOSE-like traceability from the implementation-independent SBS responsibility list down to code; the five-way decision per capability is **build / build on / reuse framework / reuse pattern / adopt standard**. Frameworks (LangGraph and similar) are treated as capabilities within a subsystem, never as subsystems. Evidence discipline: every finding carries a current-code `file:line` anchor and, for divergences, a governing-doc anchor; claims that could not be anchored were dropped. Code is the final truth about current state; owner docs govern normative intent.

---

## 1. Re-anchoring of the issue's confirmed starting findings

The issue required every starting finding to be re-anchored against current code. Result: 6 of 8 hold with refreshed line anchors; 2 materially changed because the code moved under them.

| # | Starting finding | Re-anchor result |
|---|---|---|
| 1 | Strategic core is the source-set/reproducibility/invalidation/staleness contract, not the embedding algorithm | **Holds.** `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:812-841`, `:1270-1276`; `docs/boundaries/DRI.md:28-35`, `:104-109`. |
| 2 | Part of the core exists in code (per-row provenance, doctor checks) | **Holds, anchors drifted.** Provenance stamp now `app/index/artifact_metadata.py:167-173` (was `:42/:93`); content hash `:54-61`; identity projection `:20-45`. Doctor: mixed identity `app/index/doctor.py:191-210`, metadata completeness `:216-226`, content-hash staleness `:230-241`, cache divergence `:57-88` + `:256-265`. |
| 3 | No code/schema symbol `DerivedRepresentationContract` | **Holds.** Repo-wide grep over `app/`, `schemas/`, `tests/`, `mimer_runtime/` finds no symbol; it exists only as the SBS Part 5 contract (`docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1504`) and charter output (`docs/boundaries/DRI.md:65-66`). The charter's "no tests created here" claim (`docs/boundaries/DRI.md:129-135`) has, however, **partially drifted** — see ledger row DL-5. |
| 4 | Rebuildability is partial, not subsystem-wide; D10 unvalued | **Holds, anchors drifted.** Vector event path now `app/indexer/consumer.py:100-180` (was `:93/:118/:143`); D10 still `to verify`/Open at `docs/architecture/SBS_TRANSITION_DEBT.md:32`. This audit now supplies the missing verification evidence (§4, DL-4). |
| 5 | DRI–PDM boundary crossed in legacy code; debt table points at `app/store/vector_store.py` | **Materially changed.** The formal port holds (`app/stores/base.py:55-92`), but `app/store/vector_store.py` **no longer exists** — removed by #2765 / PR #2832 (commit `36d932445`, "single store generation — remove legacy writers"). What remains at that path is `app/store/vector_index.py`, an in-memory placeholder with **no psycopg and no production callers** (only the compat re-export `app/objects/__init__.py:33` and its own docstring). Debt rows D3 (`SBS_TRANSITION_DEBT.md:25`) and D15 (`:37`) cite the deleted file as open — the register is stale (ledger DL-3). Live direct-DB residue is smaller and different: `app/store/membership_store.py:15-24` (raw `INSERT INTO membership`, called from `app/agents/projector/agent.py:36`) and the consumer-free legacy shim `app/store/relation_index.py:45-115`. |
| 6 | Contract–code divergence: `scope_binding`/`sensitivity`/`suppression_state` not preserved | **Holds, anchors drifted.** Charter: `docs/boundaries/DRI.md:89-95`. Payload builder defaults identity/role/trust/review-state/provenance only (`app/index/artifact_metadata.py:134-173`; no `scope_binding`/`sensitivity`/`suppression_state` key anywhere in the builder). Retrieval admission comment moved to `app/retrieval/hybrid.py:555-557`; the envelope synthesizes `sensitivity` from a default and hardcodes `suppression_state: "visible"` (`app/retrieval/envelope.py:135-138`). |
| 7 | Relations split between a port-based candidate path and an older direct-Postgres implementation | **Holds structurally, live-risk downgraded.** Port path: `app/stores/relation_candidates.py:141-181` → `app/stores/base.py:103-108` → `app/stores/pg.py:885-990` (`store_relations`). Legacy path `app/store/relation_index.py:27` (class) / `:64` (`neighborhood`) still exists and targets a different table (`relations`, `:48`), but has **no production caller**: only the compat re-export (`app/objects/__init__.py:32`) and tests (`tests/test_relation_index_contract.py:2`). The SIP-continuity vs DRI-projection non-separation stands (§4, DL-6/DL-11). |
| 8 | Good reusable runtime patterns: cache-through + generation token | **Holds.** Cache-through `app/retrieval/hybrid.py:127-137`, `:335-437`; generation token contract `app/stores/base.py:79-92`, pg implementation `app/stores/pg.py:804-844`; serving-path revalidation `app/retrieval/hybrid.py:302-327`. |

---

## 2. Capability-to-code matrix (AC1)

Coverage: all eleven DRI responsibilities enumerated at `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:818-830`. "Owner" is the strategic owner per the charter; "Depends on" names the boundary dependencies actually exercised in code.

| # | SBS responsibility (`SYSTEM_BREAKDOWN_STRUCTURE.md:818-830`) | Status | Code evidence | Owner | Depends on |
|---|---|---|---|---|---|
| 1 | Embeddings | **Implemented** | Event path: `app/indexer/consumer.py:137-180`; vault-changed handler: `app/services/indexer.py:103-233`; synchronous vault-ingest path: `app/ingest/vault_alpha.py:615` → `app/search/service.py:262-291`; serial queue + retry + dead-letter: `app/llm/embed_queue.py:1-30`; dimension-gated Gemini fallback: `app/llm/fallback_orchestrator.py:50-100` | DRI (pipeline), EBF (providers) | EBF (`app/components/llm/fabric.py:45`, `app/components/embeddings/legacy.py:30-67`), PDM (store ports) |
| 2 | Chunking | **Partial** | Chunk policy v1 with metadata schema: `app/ingest/chunk_policy.py:11-27`, `build_chunks` `:43-70`, structural chunker `:73-80`; but the served unit remains one row per whole note — `docs/DB_SCHEMA.md:186-190` ("rows stay keyed by whole-note `object_id` until an explicit, documented chunk-level-serving switch lands"); token/model-based windowing deferred (`app/ingest/chunk_policy.py:56-58`, #2323/W5) | DRI | EBF (provider input tolerance drives adaptive chunking, `app/llm/embed_queue.py:3-6`) |
| 3 | Lexical indexes | **Partial** | In-process BM25 only, inside the retrieval cache: `app/retrieval/hybrid.py:11` (rank_bm25 import), `:141`, `:218-219`, `:245-249`; no durable lexical index exists (no tsvector/FTS usage anywhere in `app/`) | DRI (semantics), backend replaceable | Rebuilt from durable vector-index rows' text (`app/retrieval/hybrid.py:397-413`) |
| 4 | Vector indexes | **Implemented** | Durable `store_vector_index`: DDL `app/stores/pg.py:104-115` + identity columns `:149-154`; `PgVectorIndex` `:561-883`; memory backend `app/stores/memory.py:149-243`; fail-loud backend resolution (no silent memory fallback): `app/stores/__init__.py:60-88` | DRI (index semantics), PDM (storage) | PDM (`app/stores/__init__.py:134-147` StorePort seam), EBF (identity) |
| 5 | Graph/relation projections | **Partial** | Durable `store_relations`/`store_relation_memberships`: `app/stores/pg.py:118-141`, `PgRelationIndex` `:885-990`; deterministic candidate extraction from frontmatter/body: `app/stores/relation_candidates.py:76-138`; written **only** at promotion (`app/promotion/gates.py:94-95`); consumers thin (`app/services/note_context.py:192-215`, `app/agents/pipeline.py:149-169`); **no cold-rebuild command** and **no invalidation on source change/delete** (§3 row R3); `store_relation_memberships` has no production writer or reader (grep: `add_membership`/`memberships` callers exist only in `app/stores/` and tests) | DRI (projection), SIP (relation semantics — not yet separated, DL-11) | PDM, SIP |
| 6 | Derived overlays | **Absent** | No DRI-owned overlay artifact exists. Nearest analogue is the ephemeral retrieval-envelope bundle projection (`app/retrieval/envelope.py:99-163`), which is RCA packaging, not a stored overlay | — | — |
| 7 | Machine-readable mirrors | **Absent** | No DRI-owned mirror artifact exists in `app/` (no module implements a rebuildable machine-readable mirror; grep negative) | — | — |
| 8 | Rebuild pipelines | **Partial** | Vectors: cold rebuild from `store_objects` via `python -m app.cli index rebuild` (`app/cli/index_rebuild.py:198-360`), identity convergence via `index reconcile` (`:501-620`); cache rebuild `app/retrieval/hybrid.py:335-437`; declared rebuild source `app/stores/pg.py:562`. Relations: rebuild source is *declared* (`app/stores/pg.py:886-888`: "vault frontmatter links + PgObjectStore") but **no executable rebuild path exists** | DRI | PDM, EBF |
| 9 | Staleness detection | **Implemented (vectors)** | Identity-aware generation token: `app/stores/base.py:16-26`, `:79-92`; pg implementation `app/stores/pg.py:804-844`; serving-path revalidation at most 1/s: `app/retrieval/hybrid.py:282-327`; content-hash staleness (re-embed candidates): `app/index/doctor.py:230-241` + `app/stores/pg.py:1076`; unembedded-object drift: `app/index/doctor.py:333-373`, fail-loud `IndexDriftError` `:323-330`, `verify_object_embedded` `:430-465`; cache divergence `:57-88`. Nothing equivalent for relations | DRI | PDM |
| 10 | Derived artifact invalidation | **Implemented (vectors) / Absent (relations)** | Purge on source deletion (tombstone-preserving, idempotent): `app/workers/outbox_worker.py:500-563` → `app/services/indexer.py:36`; purge on contentless source: `app/indexer/consumer.py:133-135`, TOCTOU-safe conditional purge `app/stores/pg.py:745-790`; purge-before-upsert `app/indexer/consumer.py:162`. Relations: no per-object invalidation exists — only test-scoped truncation (`app/stores/pg.py:342-366`) | DRI | PDM, EBF (events) |
| 11 | Embedding identity contracts | **Implemented** | `EmbeddingIdentity` (frozen, canonicalizing): `app/components/embeddings/legacy.py:44-67`; single-row primary identity `vector_index_meta`: `app/stores/pg.py:142-148`, `:258-305`; per-row identity columns (EMBEDREL-06): `:149-154`; ordinary upserts must match primary identity, reconcilable fallback rows recorded per-row, dim mismatch fails loud: `:588-618`; mixed-identity doctor signal `app/index/doctor.py:191-210` + `app/stores/pg.py:991`; identity-aware generation (repin moves the token, ADR-0059 D2): `app/stores/base.py:16-26` | DRI (contract), EBF (mechanics) | EBF |

Producer census for the vector write path (all `VectorIndex.upsert` call sites in `app/`): `app/indexer/consumer.py:88,180`; `app/search/service.py:282`; `app/cli/index_rebuild.py:319,710`; `app/fitness/metrics.py:111` (CI latency probe, `kind="fitness"`, deliberately bypasses the `build_indexed_unit_payload` choke with an explicit honest sentinel, `app/fitness/metrics.py:112-118`); `app/services/indexer.py` writes through the same choke at `:168-176`.

---

## 3. Cold-rebuild / source-set table per DRI record type (AC2)

| Row | Record type / store | Producer(s) | Declared source set | Configuration | Cold rebuild | Verdict |
|---|---|---|---|---|---|---|
| R1 | `store_vector_index` rows (`app/stores/pg.py:104-115`) | (a) vault ingest sync: `app/ingest/vault_alpha.py:615` → `app/search/service.py:262-291`; (b) POST /ingest event path: `app/ingest/api.py:184` → `app/workers/outbox_worker.py:472-479` → `app/indexer/consumer.py:100-180`; (c) vault-changed handler: `app/workers/outbox_worker.py:450` → `app/services/indexer.py:103-233`; (d) CLI rebuild/reconcile: `app/cli/index_rebuild.py:198-360`, `:501-620`; (e) fitness probe: `app/fitness/metrics.py:111` | Per-row provenance stamp `{source_ref, content_hash, chunk_policy_version, pipeline_version, embedding_identity}` committed in the same upsert (`app/index/artifact_metadata.py:167-173`); source text selection contract `content`→`text`→`raw_text` (`:64-80`); declared rebuild source string `app/stores/pg.py:562` | Embedding identity from EBF profile (`app/components/embeddings/legacy.py:368-377`); chunk policy `app/ingest/chunk_policy.py:24`; pipeline version `app/index/artifact_metadata.py:15` | **Verified**: `index rebuild` re-derives every row from `store_objects` payload + provider config (`app/cli/index_rebuild.py:198-360`); `store_objects` is itself rebuildable from the vault (`app/stores/pg.py:442`, `docs/DB_SCHEMA.md:176-178`) | **Rebuildable** |
| R2 | `vector_index_meta` primary identity (`app/stores/pg.py:142-148`) | First identity-bearing write (`app/stores/pg.py:276-305`) | Provider configuration only | EBF profile | Recreated by any rebuild; reset path `app/stores/pg.py:368-370` | **Rebuildable** |
| R3 | `store_relations` edges (`app/stores/pg.py:118-129`) | Promotion-time only: `app/promotion/gates.py:94-95` → `app/stores/relation_candidates.py:141-181`; edge payload carries `{source, evidence}` provenance (`:169-171`) | Frontmatter/body of the promoted note at promotion time (`app/stores/relation_candidates.py:76-138`) | None beyond the extraction patterns (`:12-27`) | **Not executable**: rebuild source is declared (`app/stores/pg.py:886-888`) but no rebuild command exists; edges are never invalidated on source edit or deletion (deletion purges only vectors, `app/workers/outbox_worker.py:511-517`) | **Possible hidden truth** — an edge set reflecting historical promotion-time frontmatter, divergent from current sources, with no reconcile path |
| R4 | `store_relation_memberships` (`app/stores/pg.py:130-141`) | None in production (`add_membership` has no callers outside `app/stores/` and tests) | n/a | n/a | n/a — dormant schema | **Dormant** (no truth accumulates; flag for reclassification or removal) |
| R5 | Legacy `relations` table (`app/store/relation_index.py:48`) | None — class has no production caller (only compat re-export `app/objects/__init__.py:32` and `tests/test_relation_index_contract.py:2`) | n/a | n/a | n/a — dormant shadow schema, distinct from `store_relations` | **Dormant shadow store**; any historical rows are unreachable by the port path |
| R6 | Legacy `membership` table (`app/store/membership_store.py:19-24`) | `app/agents/projector/agent.py:36` (live, best-effort, raw psycopg) | Implicit (projection sets) | n/a | None declared | **Possible hidden truth** + PDM bypass (D3 family) — live writer outside ports, no rebuild or read path found in `app/` |
| R7 | In-process hybrid retrieval cache (BM25 + vectors) (`app/retrieval/hybrid.py:127-271`) | Only `rebuild_from_durable_index()` (`:335-437`, "the ONLY production path allowed to populate the retrieval store"); warmed at API startup and serving path (`app/api/app.py:239-242`, `app/api/routes/ask.py:46`) | The durable index itself (KERNEL-05 cache-through) | Generation min-check interval (`:282-299`) | **Rebuildable by construction**; generation-token freshness closes the restart gap (`:302-327`) | **Rebuildable** |
| R8 | Retrieval context-cache identity (`app/retrieval/context_cache.py:1-33`) | Key derivation (MVR-03); no independent truth | Cache keyed by full context identity, never binding+generation alone | — | n/a (cache) | **Rebuildable** |
| R9 | `mimer_runtime` DRI segments (`mimer_runtime/dri.py:88-121`) | `derive_segment` — inherits full `MetadataBundle` from source, appends derivation provenance event | Explicit `derived_from` + inherited bundle (`:97-119`) | — | In-memory only ("segments are rebuildable, never durable", `:17`) | **Rebuildable** (reference implementation of the target contract) |
| R10 | Index-doctor diagnose cache (`app/index/doctor.py:91-104`) | `diagnose_index` | TTL-bounded in-process cache of a read-only diagnosis | `INDEX_DOCTOR_TTL_S` | n/a | **Rebuildable** (trivial) |

Summary: the vector chain is rebuild-verified end-to-end; the relation/graph chain and the legacy `membership` table are the concrete D10 instances (see DL-4). No DRI record type holds the only copy of human meaning — the hidden-truth risks found are projection-drift risks (stale edges, unreachable dormant rows), not knowledge-destruction risks per `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1887-1899`.

---

## 4. Boundary and responsibility conclusions (AC3)

Compared against `docs/boundaries/DRI.md:37-46` ("Does not own") and `:55-76` (Inputs/Outputs/Calls allowed), and the SBS dependency row `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1447`.

| Boundary | Charter contract | Verified code conclusion |
|---|---|---|
| **DRI–PDM** | DRI's projections are stored through PDM ports; DRI must not construct private persistence (`docs/boundaries/DRI.md:39-40`, `:86-87`) | **Conforms on the main path**: all production vector/relation writes resolve through the port seam (`app/stores/base.py:55-108`, `app/stores/__init__.py:60-147`) with fail-loud backend resolution. **Residue**: (a) `app/index/doctor.py:338-373`, `:415-421`, `:445-459` — DRI diagnosis code imports `app/stores/pg._connect` and issues raw SQL (read-only, but SQL text lives in the DRI module, not behind a port method); (b) live `app/store/membership_store.py:15-24` raw write (R6); (c) dormant shims `app/store/relation_index.py:45-115`, `app/store/vector_index.py` (no callers). The debt register's cited instance `app/store/vector_store.py` is deleted (DL-3). |
| **DRI–SIP** | Declared source sets from SIP; provenance survives derivation; SIP continuity ≠ DRI rebuildable projection (`docs/boundaries/DRI.md:57`, `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1270-1276`) | **Partially conforms.** Provenance survives derivation on the vector path (per-row stamp `app/index/artifact_metadata.py:167-173`; envelope carries `derived_from` `app/retrieval/envelope.py:149`). But there is **no explicit SIP source-set declaration surface** in `app/` — the "declared source set" is implicit in payload text-selection plus the rebuild-source strings (`app/stores/pg.py:562,886`). Relation extraction conflates SIP semantics (supports/contradicts/derived_from, `app/stores/relation_candidates.py:12`) with DRI projection mechanics in one path — the separation SBS requires is not expressed in code (DL-11). The `mimer_runtime` slice shows the intended separation (`mimer_runtime/dri.py:97-119`). |
| **DRI–RCA** | DRI supplies projections/indexes; RCA queries and decides relevance/admissibility; RCA initiates the calls (`docs/boundaries/DRI.md:42-43`, `:67-71`) | **Functionally conforms.** RCA consumes through `retrieve` → `scoped_hybrid_search` over the cache-through (`app/retrieval/capability.py:220-231`); DRI never calls RCA; ranking, scope prefiltering, evidence-role clamping, and denial packaging are RCA-side (`app/retrieval/hybrid.py:762-815`, `:24-56`). **Structural note**: the DRI machinery (BM25 lexical index, cache-through, generation freshness) is *located* in the RCA namespace `app/retrieval/hybrid.py` — a module-layout co-location, not an authority violation (RCA does not write the durable index; the doctor cross-checks divergence `app/index/doctor.py:57-88`). |
| **DRI–EBF** | DRI depends on EBF embedding/model providers but does not own the adapter (`docs/boundaries/DRI.md:45-46`) | **Conforms.** Providers resolve through the fabric intent seam (`app/components/llm/fabric.py:45`; `app/indexer/consumer.py:137-138`); identity is a frozen contract object (`app/components/embeddings/legacy.py:44-67`); provider mechanics (retry, dead-letter, dimension-gated Gemini fallback per ADR-0023) live in `app/llm/embed_queue.py` and `app/llm/fallback_orchestrator.py:50-100`, and fallback writes are recorded per-row as reconcilable (`app/stores/pg.py:588-618`). Provider identity fields stored in DRI rows are the documented provenance projection, explicitly enumerated to prevent contract widening (`app/index/artifact_metadata.py:20-45`). |
| **DRI–MEM** | MEM declares source sets into DRI and consumes "DRI memory representations" (`docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1449`, `docs/boundaries/DRI.md:57`) | **Absent.** `app/agent_memory/**` neither declares source sets into DRI nor consumes any DRI-built memory representation (grep: no `embed`/`get_vector_index` use). Provisional memory enters the RCA envelope directly as admitted items (`app/retrieval/envelope.py:114-123`), bypassing DRI entirely. This is a not-yet-built interface, not a violation — no code treats memory as a DRI source today. |

---

## 5. Divergence ledger — doc–code divergences and missing tests (AC4)

Every row carries a governing-doc anchor **and** a current-code anchor. Divergences are reported, not resolved; owner docs stay authoritative for intent.

| ID | Divergence | Governing-doc anchor | Current-code anchor |
|---|---|---|---|
| DL-1 | **Required-metadata preservation gap**: charter requires DRI to read and preserve `source_role`, `scope_binding`, `sensitivity`, `suppression_state` into projections; the production payload builder writes `source_role`/`trust`/`review_state`/provenance and has no `scope_binding`/`sensitivity`/`suppression_state` handling at all | `docs/boundaries/DRI.md:89-95` | `app/index/artifact_metadata.py:110-174` (no such keys); admission: `app/retrieval/hybrid.py:555-557` ("The live `app/` path has no suppression_state today"); downstream synthesis of defaults: `app/retrieval/envelope.py:135-138` (`suppression_state: "visible"` hardcoded) |
| DL-2 | **`DerivedRepresentationContract` exists only as prose**: SBS Part 5 names it as DRI's highest-priority stable contract; no code, schema, or test symbol exists | `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1504`; `docs/boundaries/DRI.md:65-66`, `:143-144` | Repo-wide grep negative across `app/`, `schemas/`, `tests/`, `mimer_runtime/`; nearest partial materializations: `app/index/artifact_metadata.py:167-173` (per-row provenance), `app/stores/pg.py:562,886` (`rebuild_source` strings), `app/stores/__init__.py:23-31,134-147` (`StorePortBinding.classification/rebuild_source`) |
| DL-3 | **Stale transition-debt anchors**: D3 and D15 cite `app/store/vector_store.py` as an open direct-psycopg instance; the file was deleted by #2765 / PR #2832 (commit `36d932445`). D15's "still open, no containment seam yet" statement no longer describes any existing code path | `docs/architecture/SBS_TRANSITION_DEBT.md:25`, `:37`, `:62`, `:87` | `app/store/vector_index.py` (current file at that path: in-memory placeholder, no psycopg, no production callers; compat export only at `app/objects/__init__.py:33`) |
| DL-4 | **D10 evidence now exists but the register still says `to verify`**: this audit verifies the vector chain rebuildable (R1) and identifies the actual shadow-store instances as the relation/graph chain (R3) and legacy `membership` (R6) | `docs/architecture/SBS_TRANSITION_DEBT.md:32`, `:57`, `:82` | §3 rows R1, R3, R6 anchors (`app/cli/index_rebuild.py:198-360`; `app/stores/pg.py:886-888` with no rebuild command; `app/store/membership_store.py:15-24`) |
| DL-5 | **Charter test claims drifted**: DRI.md lists three future test names and states "No tests created here"; two of the three invariants now have passing runtime enforcement under registry names, and the third has a runtime analogue — none under the charter's names | `docs/boundaries/DRI.md:129-135` | `provenance_survives_derivation` runtime test passes (`docs/testing/invariant-tests.md:149-161`; `tests/invariants/test_metadata_bundle.py:96-107`; `tests/invariants/test_dri_runtime.py:1-40`); no-naked construction guard `mimer_runtime/dri.py:43-50`; staleness: `tests/invariants/test_retrieval_spine_invariants.py:67` (`docs/testing/invariant-tests.md:531-542`). `derived_representations_rebuildable` remains without any test (missing-test entry) |
| DL-6 | **Relation edges are never invalidated**: charter requires invalidation on authority transitions/sync convergence, and policy forbids suppressed/out-of-scope material in rebuilt projections; deletion purges only vector rows, so promotion-time relation edges outlive their sources | `docs/boundaries/DRI.md:59-61`, `:99-102`; `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1396-1398` (sync flow: "DRI invalidates/rebuilds") | `app/workers/outbox_worker.py:511-517` (delete handler removes `store_vector_index` rows only); no `DELETE FROM store_relations` outside test truncation (`app/stores/pg.py:342-366`) |
| DL-7 | **Naked-vector enforcement does not reach production rows**: the registry's `store_no_naked_vectors` is schema/static-enforced against `schemas/metadata-bundle.schema.json` + `retrieval-result.schema.json`, while production `store_vector_index.payload` rows follow the different W3-SPINE-01 shape; no GATE asserts production rows carry bundle-equivalent scope/sensitivity fields — only a DOCTOR warning covers language/provenance/identity completeness | `docs/testing/invariant-tests.md:134-146`; `docs/DB_SCHEMA.md:165-200` | `tests/invariants/test_metadata_bundle.py:83-94` (schema-level assertions); `app/index/doctor.py:216-226` + `app/stores/pg.py:1029` (DOCTOR-only completeness, fields: language/provenance/identity — not scope/sensitivity) |
| DL-8 | **Two coexisting metadata models**: the bundle schema world (`scope_id`/`sensitivity`/`suppression_state`, mimer_runtime + schemas) vs the production store payload world (`domain`/`trust`/`review_state`, DB_SCHEMA); the retrieval envelope bridges them by synthesizing defaults at read time | `schemas/metadata-bundle.schema.json` (closed bundle; required semantic+provenance fields per `tests/invariants/test_metadata_bundle.py:85-88`); `docs/DB_SCHEMA.md:176-183` | `app/retrieval/envelope.py:99-163` (projection with enum defaults); `app/index/artifact_metadata.py:134-156` (producer contract) |
| DL-9 | **"Lexical indexes" responsibility has no durable artifact**: SBS lists lexical indexes as a DRI responsibility; the only lexical structure is in-process BM25 rebuilt per cache load | `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:822` | `app/retrieval/hybrid.py:11`, `:141`, `:218-219` (no durable form; grep for tsvector/FTS negative) |
| DL-10 | **"Derived overlays" and "machine-readable mirrors" are absent**: two enumerated SBS responsibilities have no code at all | `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:825-826` | No implementing module found (§2 rows 6-7) |
| DL-11 | **SIP/DRI separation not expressed in code**: SBS requires semantic continuity (SIP) and rebuildable projection (DRI) to be distinct; the single relation-candidate path owns both the semantic typing and the projection write | `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1270-1276` | `app/stores/relation_candidates.py:12-27` (semantic relation taxonomy) + `:141-181` (projection write) in one path; no SIP-owned continuity surface |
| DL-12 | **Missing test — `derived_representations_rebuildable`**: no invariant test exercises cold rebuild equivalence (rebuild → same provenance/content-hash) on any backend; rebuild correctness rests on the CLI path and operator use | `docs/boundaries/DRI.md:133`; `docs/testing/invariant-tests.md` (no entry with this scope) | `app/cli/index_rebuild.py:198-360` (untested-as-invariant); `tests/index/test_provenance_stamp.py` covers stamp shape, not rebuild equivalence |
| DL-13 | **Dormant schemas without classification**: `store_relation_memberships` (no producers/readers) and legacy `relations`/`membership` tables persist as unowned shapes; PDM charter treats unowned storage shapes as leak surface | `docs/boundaries/DRI.md:48-53` (ownership-drift rule); `docs/architecture/SBS_TRANSITION_DEBT.md:25` (D3 family) | `app/stores/pg.py:130-141` (memberships, dormant); `app/store/relation_index.py:48` (`relations`); `app/store/membership_store.py:19-24` (`membership`, live writer `app/agents/projector/agent.py:36`) |

---

## 6. Build-or-buy decision matrix (AC5)

Exactly one recommended five-way classification per capability. "Switching cost" = cost of later replacing the underlying mechanism given the recommended posture. External solutions are assessed only as capability/standard/pattern/framework candidates — no product comparison or vendor selection.

| Capability | Decision | Rationale | Switching cost under this posture |
|---|---|---|---|
| Source-set / rebuild contract (`DerivedRepresentationContract`, source-set manifest, invalidation semantics, staleness classification, metadata/provenance preservation) | **Build** | Expresses Yggdrasil's unique trust/authority model (`docs/boundaries/DRI.md:104-118`); no external artifact can own it. Partial materializations already exist to consolidate (`app/index/artifact_metadata.py:167-173`, `app/stores/base.py:79-92`, `app/stores/__init__.py:23-31`) | n/a — this is the domain core; not replaceable by design |
| Embedding pipeline orchestration (queue, retry, dead-letter, fallback gating) | **Build on** | Thin domain logic (fail-loud, per-object dead-letter, dimension-gated fallback per ADR-0023) over EBF providers (`app/llm/embed_queue.py:1-30`, `app/llm/fallback_orchestrator.py:50-100`); generic job frameworks would not carry the identity/reconcile semantics | Low — orchestration is provider-agnostic; swapping providers is an EBF concern |
| Embedding providers/models | **Reuse framework** | Already behind EBF adapters with a frozen identity contract (`app/components/llm/fabric.py:45`, `app/components/embeddings/legacy.py:44-67`); a provider library must never own subsystem boundary, source of truth, or authority semantics | Low by construction — identity columns + reconcile CLI make a model swap a governed re-index (`app/cli/index_rebuild.py:501-620`, `docs/EMBEDDINGS.md:165`) |
| Vector index backend | **Build on** | Postgres `double precision[]` with application-side similarity today (`docs/DB_SCHEMA.md:178-180`); the `VectorIndex` port (`app/stores/base.py:55-92`) is the inversion point the SBS names (`docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1481`); pgvector/ANN backends are backend choices behind the same port, not strategic domain logic (`docs/boundaries/DRI.md:39-40`) | Low-medium — port + verified cold rebuild (R1) mean a backend swap is a rebuild, not a migration of truth |
| Lexical index | **Reuse framework** | BM25 via `rank_bm25` in-process today (`app/retrieval/hybrid.py:11`); a durable lexical backend (e.g. Postgres FTS) is a mechanism candidate behind the same serving port; ranking-fusion semantics stay owned (ADR-0024 lexical≥dense hierarchy, `app/retrieval/hybrid.py:614-621`) | Low — the lexical structure is rebuilt from durable rows on every cache load; a durable replacement changes only the rebuild source |
| Chunking | **Build on** | Structural chunker + chunk-metadata schema are domain-adjacent (provenance-bearing, `app/ingest/chunk_policy.py:11-27`); tokenizer/model-aware windowing (deferred W5, `:56-58`) may reuse libraries under the same `CHUNK_POLICY_VERSION` contract | Low — policy version in every row's provenance stamp makes a chunker change a detectable, reconcilable event (`app/index/artifact_metadata.py:170`) |
| Graph/relation projection | **Build** | Relation *semantics* (supports/contradicts/derived_from) are SIP/Yggdrasil domain meaning (`app/stores/relation_candidates.py:12`); the storage shape already follows the plain adjacency-table pattern (`app/stores/pg.py:118-129`), which stays an implementation detail of the built capability. Graph frameworks/DBs are premature while consumption is thin (§2 row 5) and the rebuild/invalidation contract (R3) is unpaid | Medium — adopting a graph backend later requires the missing rebuild pipeline first; without it, migration would copy possibly-stale hidden truth |
| Retrieval-cache serving substrate (cache-through + generation token) | **Reuse pattern** | Cache-through of a durable index with an opaque generation token is an established, verified pattern here (KERNEL-05/G1res-1: `app/retrieval/hybrid.py:127-137`, `:302-327`; `app/stores/base.py:79-92`); it is the backend-independent staleness mechanism the issue's finding 8 names | Low — pattern is substrate-agnostic; any future serving substrate re-implements `generation()` equality |
| Doctor / reconcile machinery | **Reuse pattern** (explicit doctor/reconcile) | Read-only diagnosis + explicit operator repair is a deliberate pattern (`app/index/doctor.py:107-125`; `app/cli/index_rebuild.py:501-620`); generic data-quality frameworks would not know the identity/provenance semantics | Low |
| Derived lineage vocabulary | **Adopt standard (candidate)** | Evaluate W3C PROV as a *mapping target* for the provenance stamp (`app/index/artifact_metadata.py:167-173`) and `derived_from` lineage (`app/retrieval/envelope.py:149`, `mimer_runtime/dri.py:110`) — mapped to Yggdrasil semantics, never adopted as the normative domain model | Low — a mapping layer; internal stamp remains authoritative |
| Rebuild/staleness observability | **Adopt standard (candidate)** | Evaluate OpenTelemetry as the *transport* for rebuild/staleness observations (doctor signals `app/index/doctor.py:290-310`, rebuild logs `app/retrieval/hybrid.py:426-432`) — OEF concern; DRI keeps the signal semantics | Low — emission-side adapter only |

---

## 7. Invariant extraction — MUST / GATE / DOCTOR (AC6)

This section **extends the existing registry semantics** (`docs/testing/invariant-tests.md:54-68` enforcement categories) and cross-references its entries by name. It creates no competing register: every proposal below is phrased as an extension of a named existing entry, per the charter's invariant list (`docs/boundaries/DRI.md:111-118`).

### 7.1 No naked derived representation

Registry entries: `store_no_naked_vectors` (`docs/testing/invariant-tests.md:134-146`), `provenance_survives_derivation` (`:149-161`).

| Category | Status | Evidence |
|---|---|---|
| MUST (fail-loud at runtime) | **Exists — keep (partial)** | Segment construction guard `mimer_runtime/dri.py:43-50`; production upsert fails loud on dim/model/identity mismatch and refuses stampless legacy vectors whose bytes are not canonical (`app/stores/pg.py:588-618`; `app/indexer/consumer.py:72-78`); every builder-covered sink stamps provenance in the same upsert (`app/index/artifact_metadata.py:158-173`) |
| GATE (CI-blocking) | **Exists — keep (schema scope only)** | `tests/invariants/test_metadata_bundle.py:83-94` (closed bundle schema; candidate items require bundles); store-payload census for builder-covered sinks (`tests/properties/test_store_payload_episode_ref.py` per `app/index/artifact_metadata.py:147-153`); **gap (DL-7)**: no GATE asserts production `store_vector_index` rows carry bundle-equivalent scope/sensitivity fields — extend `store_no_naked_vectors`'s eventual test path, do not add a new invariant |
| DOCTOR (read-only reconciliation) | **Exists — keep** | Metadata/provenance completeness listing (`app/index/doctor.py:216-226`, `app/stores/pg.py:1029`); unembedded-object drift (`app/index/doctor.py:333-373`) |

### 7.2 Metadata preservation

Registry entry: `provenance_survives_derivation` (`docs/testing/invariant-tests.md:149-161`); charter clause `docs/boundaries/DRI.md:89-95`.

| Category | Status | Evidence |
|---|---|---|
| MUST | **Violated today** (production path) | The write-path choke preserves `source_role`/`trust`/`review_state`/provenance but not `scope_binding`/`sensitivity`/`suppression_state` (`app/index/artifact_metadata.py:110-174`; DL-1). The mimer_runtime reference implements the full inheritance (`mimer_runtime/dri.py:97-119`) — the MUST is "promote that inheritance into the production choke once producers stamp the fields" |
| GATE | **New** (as an extension of `provenance_survives_derivation`'s test path) | Runtime test passes for the reference slice (`tests/invariants/test_metadata_bundle.py:96-107`, `tests/invariants/test_dri_runtime.py`); no gate covers the `app/` production payload |
| DOCTOR | **Exists — keep (partial)** | Completeness check covers language/provenance/identity only (`app/stores/pg.py:1029-1075`); extending its field list to the preserved-dimension set is the DOCTOR-side counterpart of DL-1 |

### 7.3 Stale-before-serve

Registry entry: `retrieval_serves_durable_truth_fresh` (`docs/testing/invariant-tests.md:531-542`); charter failure mode "stale-as-current" (`docs/boundaries/DRI.md:125`).

| Category | Status | Evidence |
|---|---|---|
| MUST | **Exists — keep** | Serving path revalidates the cache against the identity-aware durable generation token at most once per bounded interval and force-rebuilds on mismatch (`app/retrieval/hybrid.py:302-327`; `app/stores/base.py:79-92`; identity repin moves the token, `app/stores/base.py:16-26`) |
| GATE | **Exists — keep** | `tests/invariants/test_retrieval_spine_invariants.py:67` (runtime, passes) |
| DOCTOR | **Exists — keep** | Content-hash staleness listing (`app/index/doctor.py:230-241`, `app/stores/pg.py:1076`); cache/durable divergence check (`app/index/doctor.py:57-88`, `:256-265`); explicit repair via `index reconcile` (`app/cli/index_rebuild.py:501-620`) |

**Minimal kernel** for DRI's correctness claims: (1) the 7.3 MUST (generation-checked cache-through), (2) the 7.1 MUST (same-statement provenance stamp + fail-loud identity gates), (3) the vector cold-rebuild path (R1). Everything else in this section is defense in depth. The two open items that carry real risk are the 7.2 MUST (metadata preservation on the production path) and the relation-chain rebuild/invalidation absence (R3/DL-6) — which sits outside all three invariants today and is the largest unenforced surface.

---

## 8. SBS reconciliation

Binding classification of every structural proposal implied above. The audit itself changes no SBS.

| Proposal (advisory) | SBS classification |
|---|---|
| Materialize `DerivedRepresentationContract` as a code/schema symbol consolidating the provenance stamp, rebuild-source declarations, and store classification | **Conforms to** SBS (Part 5 already names it, `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1504`) |
| Add a relation-projection rebuild pipeline and per-object invalidation (closing R3/DL-6) | **Conforms to** SBS (Rebuild pipelines / Derived artifact invalidation are enumerated DRI responsibilities, `:827-829`) |
| Preserve `scope_binding`/`sensitivity`/`suppression_state` through the production write choke (closing DL-1) | **Conforms to** the DRI charter (`docs/boundaries/DRI.md:89-95`) |
| Reconcile the two metadata models (bundle schema vs W3-SPINE-01 payload, DL-8) into one preserved-dimension contract | **Extends** SBS — the SBS names the metadata dimensions but not which concrete schema carries them in `store_vector_index`; requires CES-stewarded contract work, not silent convergence |
| Relocate or re-badge the DRI machinery living in `app/retrieval/hybrid.py` (BM25, cache-through, generation check) under an explicitly DRI-owned module | **Extends** SBS (module layout is not an SBS concern; flagged for CES/module-ownership hygiene only) |
| Refresh transition-debt rows D3/D10/D15 with this audit's evidence (DL-3/DL-4) | **Conforms to** the register lifecycle (`docs/architecture/SBS_OPERATING_MODEL.md` §10) |
| Classify or remove dormant schemas (`store_relation_memberships`, legacy `relations`, `membership`) (DL-13) | **Conforms to** the DRI ownership-drift rule (`docs/boundaries/DRI.md:48-53`) and PDM charter |
| Introduce a durable lexical index or declare the in-process BM25 the intended form (DL-9) | **Conforms to** SBS either way (`:822` names the responsibility; the SBS does not mandate durability) — a charter clarification decision |
| W3C PROV / OpenTelemetry adoption | **Extends** SBS as adapter-level candidates (consistent with `standards_are_adapters`, `docs/testing/invariant-tests.md:466`); never a normative domain model |

No proposal in this audit **proposes reshaping** the SBS. Reshape-class questions found: none — the DRI boundary as chartered matched every observed mechanism; all gaps are unimplemented responsibilities or preservation debts, not boundary misdraws.

---

## 9. Prioritized audit plan for subsequent subsystems (AC7)

Advisory only — the order below refines the issue's proposed order with this pass's evidence and **requires SBS-owner confirmation before any next slice is opened** (issue #3539 Out of Scope). Stop condition for this slice: DRI's evidence matrix is complete (§2-§5) — satisfied.

| # | Next subsystem slice | Why next (dependency) | Entry evidence from this pass | Stop conditions for that slice |
|---|---|---|---|---|
| 1 | **SIP–HKA–DRI boundary** (semantic continuity vs projection) | DL-11 and R3 show the SIP/DRI split is unexpressed exactly where relations are produced; DL-1/DL-8 need the SIP-side answer to "which dimensions must producers stamp" before DRI can preserve them | `app/stores/relation_candidates.py:12-27`; `app/index/artifact_metadata.py:110-174`; `docs/SYSTEM_BREAKDOWN_STRUCTURE.md:1270-1276` | Stop when: every semantic dimension has a named producer or a documented absence; do not proceed to PDM until the DL-8 metadata-model question has an owner decision |
| 2 | **PDM** (storage ownership, ports, migrations) | DL-3 shows the debt register has drifted from code; the port seam (`app/stores/__init__.py:60-147`) is load-bearing for every DRI conclusion here and needs its own inventory (DSN construction, `_ensure_tables` autocreate vs Alembic, `app/stores/pg.py:71-158` vs `app/alembic/versions/c2766a04d001*`) | `app/store/membership_store.py:15-24`; `app/stores/provider.py:23-56` (duplicate backend-resolution logic); `app/index/doctor.py:338-373` (raw SQL in DRI module) | Stop when: every direct-DSN/table construction is inventoried with a live/dormant/deleted status; refuse to conclude while D3's "broader extent `to verify`" remains uninspected |
| 3 | **RCA** (retrieval, ranking, envelope) | Consumes DRI's outputs; DL-7/DL-8 read-side synthesis (`app/retrieval/envelope.py:99-163`) is where preserved-vs-defaulted metadata becomes user-visible | `app/retrieval/capability.py:220-231`; `app/retrieval/hybrid.py:762-815` | Stop when: every envelope field is classified preserved/synthesized/absent with producer anchors |
| 4 | **MEM** | §4 shows the DRI–MEM interface is entirely unbuilt; MEM's audit must decide whether memory representations enter DRI or stay envelope-admitted | `app/retrieval/envelope.py:114-123`; grep-negative `app/agent_memory/**` | Stop when: the memory-representation line (DRI-built vs direct-admitted) has an owner decision recorded |
| 5 | **EBF** | DRI's provider seam is healthy (§4) but D8 ("provider fields in core contracts") remains `to verify` at register level | `app/index/artifact_metadata.py:20-45`; `docs/architecture/SBS_TRANSITION_DEBT.md:30` | Stop when: D8's field audit is complete across HKA/SIP/GOV public contracts |
| 6-10 | **GOV → CAO–EXE → SFC → HIX/OEF/CES cross-cut** | Unchanged from the issue's advisory order; nothing in this pass reorders them | `docs/architecture/SBS_TRANSITION_DEBT.md` rows D2, D6, D7, D9 | Per-slice charters to be set by the SBS owner |

---

## 10. Follow-up contracts (advisory)

Per the issue's validation requirement, each implementation-shaped finding names a concrete follow-up contract. None is enacted here; promotion runs through `docs-to-issue` / `feature-breakdown` with the anchors in this audit as source anchors.

1. **Transition-debt register refresh (D3/D10/D15)** — re-anchor D3/D15 off the deleted `app/store/vector_store.py`, record the #2765 removal, split D10 into verified-vector vs open-relation halves using §3. (Register-owner doc change; smallest slice; evidence: DL-3, DL-4.)
2. **Relation projection rebuild + invalidation** — executable rebuild from current vault frontmatter, per-object edge invalidation on delete/update; extends `derived_representations_rebuildable`. (Evidence: R3, DL-6, DL-12.)
3. **Metadata preservation on the production write choke** — thread `scope_binding`/`sensitivity`/`suppression_state` from producers through `build_indexed_unit_payload` into `store_vector_index.payload`, then remove the envelope's hardcoded defaults; requires the SIP slice's producer decision first. (Evidence: DL-1, DL-8; blocked on plan row 1.)
4. **`DerivedRepresentationContract` materialization** — one schema/code symbol consolidating provenance stamp, rebuild-source declaration, and store classification; CES-stewarded. (Evidence: DL-2.)
5. **Dormant-schema disposition** — classify or remove `store_relation_memberships`, legacy `relations`, legacy `membership` (+ its live projector writer). (Evidence: DL-13, R4-R6.)
6. **Rebuild-equivalence invariant test** — a runtime test that cold-rebuilds a seeded corpus and asserts provenance/content-hash equivalence; fills the only charter-named invariant with no enforcement. (Evidence: DL-5, DL-12.)
