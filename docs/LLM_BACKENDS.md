State: Deprecated. This document is retained as a redirect stub during the docs refactor.
# LLM Backends

`docs/LLM.md` is now the primary operational document for LLM providers, backend capabilities, environment variables, and local setup.

Keep using `docs/LLM_ROUTING.md` for the routing/fabric contract.

This file remains only to preserve old links during the refactor. Do not treat it as a separate active policy surface.
