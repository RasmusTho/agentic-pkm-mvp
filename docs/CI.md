# CI in WS
WS CI runs:
- yamllint for `data/context/`
- a smoke test: brings up docker compose, checks `/query` returns citations
Legacy notes about Ruff/mypy/pytest do not apply to WS; they will return in later milestones.
