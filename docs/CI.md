State: Deprecated CI/testing redirect. CI and fitness-gate guidance now lives in `docs/TESTING.md`.
# Continuous Integration — SoT v5.5

This document is retained only as a compatibility stub during the docs cleanup.

Use:
- `docs/TESTING.md` for CI-aligned validation, fitness gates, smoke slices, and known testing gaps
- `docs/eval.md` for opt-in eval suites

This file should be removed once inbound links are fully updated.
