State: Deprecated CLI-reference redirect. Stable operator CLI guidance now lives in `docs/OPERATIONS.md`; command discovery remains `python -m app.cli --help`.
# CLI Reference

This document is retained only as a compatibility stub during the docs cleanup.

Use:
- `docs/OPERATIONS.md` for stable operator-facing commands and examples
- `docs/HUMAN-FLOWS.md` for flow semantics behind `ask` and watcher-driven UX
- `docs/DEV_WORKFLOW.md` for development/test workflow expectations
- `python -m app.cli --help`
- `python -m app.cli <command> --help`

This file should be removed once inbound links are fully updated.
