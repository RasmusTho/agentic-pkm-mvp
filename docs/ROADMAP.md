# Roadmap

## v4.5B — Unified Ingestion Polish
Chunking policy added and deduper wired in pipeline. Duplicates emit markers for reviewer decisions. Default sizes are conservative to keep embeddings stable. Performance targets and determinism remain unchanged in memory mode.
