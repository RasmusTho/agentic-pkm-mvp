State: Deprecated testing-quality redirect. Validation guidance now lives in `docs/TESTING.md`; runtime safety policy lives in `docs/guardrails.md`.
# Quality & Guardrails

This document is retained only as a compatibility stub during the docs cleanup.

Use:
- `docs/TESTING.md` for quality expectations as part of validation
- `docs/guardrails.md` for the runtime safety policy itself
- `docs/eval.md` for opt-in quality/eval suites

This file should be removed once inbound links are fully updated.
