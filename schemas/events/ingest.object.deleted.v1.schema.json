{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yggdrasil.local/schemas/events/ingest.object.deleted.v1.schema.json",
  "title": "ingest.object.deleted payload (v1)",
  "description": "Payload of the ingest.object.deleted outbox event. Producer: app/services/vault_sync.py::delete_note (delete_payload: path, deleted, reason, source, uuid). Consumer: app/workers/outbox_worker.py::handle_ingest_object_deleted, a logging no-op cleanup hook (no strict field requirement at consume time today), so this schema pins the shape the producer actually emits without over-constraining the consumer.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "uuid": { "type": ["string", "null"], "description": "Canonical lifecycle object id." },
    "object_id": { "type": ["string", "null"], "description": "Canonical store_objects identity." },
    "vault_uuid": { "type": ["string", "null"], "description": "Retained frontmatter continuity identity." },
    "path": { "type": ["string", "null"] },
    "deleted": { "type": "boolean" },
    "reason": { "type": ["string", "null"] },
    "source": { "type": ["string", "null"] }
  }
}
