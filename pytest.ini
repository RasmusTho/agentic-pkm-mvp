[pytest]
addopts = -q --timeout=20 --timeout-method=thread
markers =
    integration: tests that require Postgres
    pg: store-contract tests that require Postgres backend
    not_pg: tests that must not touch Postgres or external deps
pythonpath = .
