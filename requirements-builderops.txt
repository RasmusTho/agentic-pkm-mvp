# BuilderOps control-plane runtime closure.
# Keep this manifest separate from Product's media/retrieval/LLM stack: the
# image boot paths are app.builderops.control_plane.{service,worker,migrate}
# and the standalone app.builderops.devui_runtime; receipt CLI is pure.
fastapi==0.136.3
jsonschema==4.23.0
psycopg[binary]==3.2.10
pydantic==2.12.0
starlette==1.2.1
uvicorn==0.37.0
